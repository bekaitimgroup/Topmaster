{
  "name": "topmaster",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter './apps/*' dev",
    "dev:web": "pnpm --filter @topmaster/web dev",
    "dev:api": "pnpm --filter @topmaster/api start:dev",
    "build": "pnpm --filter './apps/*' build",
    "lint": "pnpm --filter './apps/*' lint"
  }
}
